services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    platform: linux/arm64/v8
    working_dir: /workspace
    volumes:
      - ./:/workspace
    tty: true
    stdin_open: true
    command: bash
